# docker

Container definitions and Compose files for local development and deployment
(Postgres, Redis, Qdrant, backend, ai). Populated in **Phase 3+**.

Goal: `docker compose up` brings the full stack online. See
[`../docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md).
